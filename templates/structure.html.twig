{% extends 'base.html.twig' %}

{% block body %}
	<div class="container">
		<header>
			<h1 class="text-center">FootPronos</h1>
		</header>
		<section>
			<div class="row">
				<div class="col-2">
					<ul class="nav flex-column">
						<li class="nav-item">
							<a href="{{ path('home') }}">Accueil</a>
						</li>
						{% if is_granted('ROLE_ADMIN') %}
							<li class="nav-item">
								<a href="{{ path('new_resultat') }}">Ajouter résultat</a>
							</li>
						{% endif %}	
						<li class="nav-item">
							<a href="{{ path('pronos') }}">Mes pronos</a>
						</li>					
						<li class="nav-item">
							<a href="{{ path('logout') }}">Déconnexion</a>
						</li>
					</ul>
				</div>
				<div class="col-10">
					{% block content %}{% endblock %}
				</div>
			</div>
		</section>
	</div>
{% endblock %}